export default function (state = {}, action) {
    // console.log(`action = ${JSON.stringify(action)}`);
    // console.log('authReducer');
    console.log(action);
    switch (action.type) {
        default:
            return state;
    }
}
